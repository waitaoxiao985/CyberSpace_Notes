# Python 多进程
- ## 多进程
  - ### 通过应用程序利用计算机的多核资源达到同时执行多个任务的目的, 以此来提升程序的执行效率 
    ```python
    #!/usr/bin/env python

    import os
    from multiprocessing import Process

    def hello(name):
        os.getpid()
        print('process {}' . format(os.getpid()))
        print('Hello' + name)

    ## hello('World!')

    def main():
        ## main() 函数运行在主程序中
        print('process {}' . format(os.getpid()))
        p = Process(target = hello, args = ('world', ))
        print('child process start')
        p.start()
        p.join() ## 阻塞主进程, 直到子进程终止
        print('child process stop')

    if __name__ == '__main__':
        main()  
    ```
- ## 提高程序运行的效率
  - ### 计算机有运行器和存储器
    - #### 四核, 八核指的是 CPU 数量
  - ### 单进程
    ```python
    import time
    def io_task():
        time.sleep(1)

    def main():
        start_time = time.time()
        for i in range(5):
            io_task()

        end_time = time.time()
        print('程序运行消耗 {:2f}' . format(end_time - start_time))
      
    if __name__ == '__main__':
        main()
    ```
  - ### 多进程
    ```python
    import time
    from multiprocessing import Process 
    def io_task():
        time.sleep(1)

    def main():
        start.time = time.time()
        process_list = []
        for i in range(5):
            process_list.append(Process(target=io_task))
            
        for pro in process_list:
            pro.start()

        for pro in process_list:
            pro.join()

        end_time = time.time()
        print('程序运行耗时 {:.2f}' . format(end_time - start_time))
      
    if __name__ == '__main__':
        main()
    ```
  - ### 查看 CPU 数量
    ```python
    from multiprocessing import cpu_count
    cpu_count()
    ``` 
- ## 进程间通信
  - #### 进程有自己独立的运行空间, 它们之间所有的变量, 数据是隔绝的, A 进程中的变量 test 与 B 进程的变量没有任何关系, 这就意味要使用一些特殊的手段才能实现它们之间的数据交换
  - ### Pipe
    ```python
    #!/usr/bin/env python
    from multiprocessing import Process, Pipe
    conn1, conn2 = Pipe()

    def send():
        data = 'handsome xxx'
        conn1.send(data)
        print('Send Data: {}' . format(data))

    def recv():
        data = conn2.recv()
        print('Receive Data: {}' . format(data))

    def main():
        Process(target=send).start()
        Process(target=recv).start()

    if __name__ == '__main__':
        main()
    ```
  - ### Queue 
    ```python
    #!/usr/bin/env python
    from multiprocessing import Process, Queue
    
    queue = Queue()

    def f1(q):
        i = 'handsome xxx'
        q.put(i)
        print(f'Send Data: {i}')

    def f2(q):
        data = q.get()
        print(f'Receive Data: {data}')

    def main():
        Process(target = f1, args = (queue, )).start()
        Process(target = f2, args = (queue, )).start()

    if __name__ == '__main__':
        main()
    ```
- ## 进程锁
  - #### 在python的多线程和多进程中，当我们需要对多线程或多进程的共享资源或对象进行修改操作时，往往会出现因cpu随机调度而导致结果和我们预期不一致的问题，这时就需要对线程或者进程加锁，以保证一个线程或进程在对共享对象进行修改时，其他的线程或进程无法访问这个对象，直至获取锁的线程的操作执行完毕后释放锁。所以，锁在多线程和多进程中起到一个同步的作用，以保护每个线程和进程必要操作的完整执行
  - ### 多进程共享变量操作
    ```python
    import time
    from multiprocessing import Process, Value
    def func(val):
      ## 该函数运行在子进程中, 参数 val 是一个 Value 对象, 全局变量
        for i in range(50):
            time.sleep(0.01)
            val.value += 1

    if __name__ == '__main__':
        val = Value('i', 0)
        procs = [Process(target = func, args = (val, ) for _ in range(10)]
        for _ in procs:
            _.start()

        for _ in procs:
            _.join()

        print(val.value)
    ```
  - ### 某一时刻某一进程获得锁 (增加安全性)
    ```python
    import time
    from multiprocessing import Process, Value, Lock
    def func(val, lock):
      ## 该函数运行在子进程中, 参数 val 是一个 Value 对象, 全局变量
        for i in range(50):
            time.sleep(0.01)
            lock.acquire()
            val.value += 1
            lock.release()

    if __name__ == '__main__':
        val = Value('i', 0)
        lock = Lock()
        procs = [Process(target = func, args = (val, lock)) for _ in range(10)]
        for _ in procs:
            _.start()

        for _ in procs:
            _.join()

        print(val.value)
    ```
- ## 进程池
  - #### 进程池的概念，定义一个池子，在里面放上固定数量的进程，有需求来了，就拿一个池中的进程来处理任务，等到处理完毕，进程并不关闭，而是将进程再放回进程池中继续等待任务。如果有很多任务需要执行，池中的进程数量不够，任务就要等待之前的进程执行任务完毕归来，拿到空闲进程才能继续执行。也就是说，池中进程的数量是固定的，那么同一时间最多有固定数量的进程在运行。这样不会增加操作系统的调度难度，还节省了开闭进程的时间，也一定程度上能够实现并发效果
    ```python
    #!/usr/bin/env python

    import os, time, random
    from multiprocessing import Pool

    def task(name):
        ## 打印进程 id
        print(f'process: {name} 启动运行, id: {os.getpid()}')
        start_time = time.time()
        ## 生成一个随机数
        time.sleep(random.random() * 3)
        end_time = time.time()
        print('任务{}结束运行, 耗时: {:.2f} 秒' . format(name, end_time - start_time))

    if __name__ == '__main__':
        print('当前是主进程, id {}' . format(os.getpid()))
        print('==============================')
        p = Pool(4)
        for _ in range(1, 6):
            ## 异步方式启动进程池
            p.apply_async(task, args(_, ))

        p.close()
        print('开始运行子进程')
        p.join()
        print('==============================')
        print('子进程运行完毕, 当前是主进程, id: {}' . format(os.getpid()))
    ```
- ## 多线程
  - #### 线程是CPU分配资源的基本单位。当一程序开始运行，这个程序就变成了一个进程，而一个进程相当于一个或者多个线程。当没有多线程编程时，一个进程相当于一个主线程；当有多线程编程时，一个进程包含多个线程(含主线程)。使用线程可以实现程序大的开发。多个线程可以在同一个程序中运行，并且每一个线程完成不同的任务。多线程实现后台服务程序可以同时处理多个任务，并不发生阻塞现象。多线程的程序设计的特点就是能够提高程序执行效率和处理速度
    ```python
    #!/usr/bin/env python
    import threading
    def hello(name):
        #get_ident() 获取当前线程id
        print(f'当前为子进程, id {threading.get_ident()}')
        print('hello' + name)

    def main():
        print(f'当前为主进程, id {threading.get_ident()}')
        print('========================================')
        t = threading.Thread(target = hello, args = ('world', ))
        t.start()
        t.join()
        print('========================================')
        print(f'当前为主进程, id {threading.get_ident()}')

        # 输出:
        # 当前为主进程, id 139914385371648
        # ========================================
        # 当前为子进程, id 139914375067328
        # helloworld
        # ========================================
        # 当前为主进程, id 139914385371648
    ```
---
# Python 常用模块
- ## os
  - ### 操作系统相关的操作
    ```python
    os.name
    filename='/home/kali/Desktop/python_code/1.txt'
    os.path.abspath(filename) ## 返回文件绝对路径
    os.path.basename(filename) ## 返回文件名
    os.path.isfile(filename)
    os.path.isdir(filename)
    os.mkdir()
    os.rmdir()
    os.rename(`/home/kali/Desktop/python_code/1.txt`, 2.txt)
    ```
- ## datetime
  - ### 时间日期及相关计算
    ```python
    from datetime import date, datetime
    date.today()
    datetime.now() ## 容易出现时间校验漏洞
    datetime.now().year
    datetime.now().month
    datetime.now().day
    datetime.now().hour
    datetime.now().minute
    datetime.now().second
    print(datetime.now().time())
    ```
- ## time
  - ### 处理时间、打印当前时间、强制挂起当前进程
    ```python
    import time
    time.localtime()
    print(time.localtime().tm_yday) ## 今年过了多少天
    time.time()
    ```
- ## requests
  - ### 网络请求标准库
    ```python
    import requests
    r = requests.get('https://www.baidu.com')
    r.status_code  ## 200
    r.headers['content-type']
    r.text

    r = requests.get('https://aip.github.com')
    r.json()
    ```
- ## base64
  - ### 用字符表示二进制数据
    ```python
    import base64
    base64.b64encode(b'handsome xxx') ## 加密操作, 反之亦然
    ```
- ## copy
  - ### 深复制与浅复制，复制可变数据类型
    ```python
    import copy

    # 浅拷贝
    l = ['1', '2', '3', 'ada']
    l2 = copy.copy(l)
    l2.append('1234') ## 只是拷贝, l数组的值不会改变

    # 深拷贝
    l1 = [1, [2, 3]]
    l3 = copy.deepcopy(l1)
    l3[-1].append(4) ## 深拷贝, l和l3都改变了
    ```
- ## configparser
  - ### 读取配置文件
    - #### .ini, .json, .xml, .yaml
    ```ini
    [user]
    name = wuyue
    id = 9527
    date = 2026-3-18

    [courses]
    python = python1
    mobile = mobile1
    hw = hvv1
    ```

    ```python
    from configparser import ConfigerParser
    config = ConfigParser
    config = ConfigParser()
    config.read('wy.ini', encoding = 'UTF-8')
    config.sections() ## 获取配置文件的配置组
    config.options('user')
    config.items('user')
    config.get('user', 'name')
    config.add_section('permission')
    config.set('permission', 'isMember', 'Ture')
    with open('wuyue.ini', 'w', encoding = 'UTF-8') as f:
            config.write(f)
    ```
- ## collections
  - ### 提供一系列特殊的容器类
    ```python
    from collections.abc import Iterator, Iterable
    isinstance('hello', Iterator)
    ## False
    isinstance('hello', Iterable)
    ## True

    from collections.abc import Generator
    isinstance(iter('hello'), Generator)
    ## false
    g = (i ** 2 for i in range(10))
    isinstance(g, Generator)

    from collections import OrderedDict
    d = OrderedDict()
    d['apple'] = 1
    d['geogle'] = 2
    d['ms'] = 3
    d['alibaba'] = 4
    for _ in d:
        print(_.d[_])

    import json
    json.dumps(d)
    from collections import namedtuple
    Print = namedtuple('Point', ['x', 'y'])
    p = Print(10, 12)
    p.x
    p.y
    from collections import Counter
    print(Counter('http://www.baidu.com'))
    ```
- ## ==re正则表达式==
  - ### findall 函数
    - #### 找到匹配的所有子串, 并返回一个列表, 如果没有匹配到就为空
    ```python
    import re
    s = 'hello xxx 520 xxx'
    re.findall('xxx', s)
    re.findall('520', s)
    ```
  - ### search 函数
    - #### 扫描整个字符串, 并且返回第一个, 如果没有匹配到就为空
  - ### 匹配元字符
    - #### `\d`
      - ##### 获取所有数字 0 - 9
    - #### `\D`
      - ##### 匹配非数字
    - #### `\w`
      - ##### 匹配单词字符, 包括大小写, 数字, 下划线, 中文
    - #### `\W`
      - ##### 匹配剩下的, 空格, 换行符, 特殊字符
  - ### 匹配字符集
    - #### 字符的集合, 用 [] 表示, 字符集内用 "^" 表示 "非"
      - ##### \d = [0 - 9]
      - ##### \D = [^0 - 9]
      - ##### \w = [a-zA-Z_0-9]???
  - ### 匹配空白符
    - #### ' ' 空格
    - #### \n 换行符
    - #### \t 制表符
    - #### \r 回车符
    - #### \s 匹配空白符
      - ##### 过滤思想: `re.findall('[^\s]', xxx)`将空白符去除
  - ### {}
    - #### 表示匹配标定字符的数量
    - #### `re.findall('[a-z]{3}', s)`
  - ### 特殊数量符号
    - #### * 
      - ##### 匹配前一个字符 0 或者 无限次
    - #### +
      - ##### 匹配前一个字符 1 或者 无限次
    - #### .
      - ##### 匹配除去换行符 \n 以外的任意 1 个字符
    ```python
    s = 'hell#hello$helloo@'
    re.findall('hello*', s)
    ## ['hell', 'hello', 'helloo']
    re.findall('hello+', s)
    ## ['hello', 'helloo']
    ```
  - ### 字符组
    - #### 把字符用 () 括起来, 叫字符组, 目的就为将匹配成的字符串分组 
    - `re.findall('\s*(\w+)\s*', s)`
  - ### 匹配参数模式
    - #### findall方法的第三个参数为默认参数
    - #### `re.I`
      - ##### 忽略大小写
      - ##### `re.findall('Lo', s, re.I)`
    - #### `re.S`
      - ##### 匹配空白符
      - ##### `re.findall('Lo', s, re.S)`
- ## ==subprocess==
  - ### 多进程协同
    - #### os.system 函数
      - ##### 阻塞式
    - #### multiprocessing 模块
      - ##### 计算机密集型计算适用
    - #### subprocess 模块
      - ##### subprocess.run
        - ###### 执行指定的命令, 等待命令执行完成后返回一个对象
        | 参数名 | 类型 | 默认值 | 简要说明 |
        | :--- | :--- | :--- | :--- |
        | ==args== | 字符串或列表 | **必需** | 要执行的命令及其参数。 |
        | ==stdin== / `stdout` / `stderr` | 文件对象、`PIPE`、`DEVNULL` | `None` | 控制标准输入、输出和错误的流向。 |
        | `input` | 字节串或字符串 | `None` | 直接传递给子进程的标准输入数据。 |
        | `capture_output` | 布尔值 | `False` | 自动捕获 `stdout` 和 `stderr`，等价于同时设置 `stdout=PIPE, stderr=PIPE`。 |
        | ==shell== | 布尔值 | `False` | 是否通过 shell 解释器执行命令。 |
        | `cwd` | 字符串 | `None` | 设置子进程的当前工作目录。 |
        | ==timeout== | 浮点数 | `None` | 设置子进程执行的超时时间（秒）。 |
        | `check` | 布尔值 | `False` | 若为 `True`，当子进程返回非零状态码时，抛出 `CalledProcessError` 异常。 |
        | `encoding` | 字符串 | `None` | 指定输出文本的编码（如 `'utf-8'`）。 |
        | `errors` | 字符串 | `None` | 指定解码错误时的处理策略（如 `'ignore'` 或 `'replace'`）。 |
        | `text` (或 `universal_newlines`) | 布尔值 | `False` | 若为 `True`，将标准输入、输出、错误以文本模式（字符串）处理，而不是字节。 |
        | `env` | 字典 | `None` | 为子进程指定自定义环境变量，替代继承父进程的环境。 |

      - ##### subprocess.call
        - ###### 执行指定的命令, 返回命令执行的状态
        ```python
        import subprocess
        subprocess.call(['ls', '-al'])
        ## 最后成功输出 0, 失败输出 非0
        ```
      - ##### subprocess.check_call
        - ###### 类似上面的, 区别是它会输出报错
        ```python
        subprocess.check_call(['ls', '-al'])
        ## 失败了会输出具体报错
        ```
      - ##### subprocess.getoutput
        - ###### 执行命令, 返回结果
      - ##### subprocess.Popen (最底层)
        ```python
        # 底层代码
        import subprocess
        process = subprocess.Popen(
            cmd,
            shell = True, ## 打开终端, Linux中是终端, Windows中是cmd
            stdout = subprocess.PIPE ## 如果该命令执行成功, 那么将该命令的标准输出放入管道
            stdin = subprocess.PIPE ## 如果该命令执行成功, 那么将该命令的标准输入放入管道
            stderr = subprocess.PIPE ## 如果该命令执行成功, 那么将该命令的标准报错放入管道
        )

        # 示例代码

        #!/usr/bin/env python
        import subprocess
        obj = subprocess.Popen(['python'], stdin = subprocess.PIPE, stdout = subprocess.PIPE, stderr = subprocess.PIPE, text = True) 
        ## universal_newlines 在 python 3.6 之后就已弃用的参数
        obj.stdin.write('print(1)\n')
        obj.stdin.write('print(2)\n')
        obj.stdin.close()

        cmd_out = obj.stdout.read()
        obj.stdout.close()
        cmd.err = obj.stderr.read()
        obj.stderr.close()

        print(cmd_out)
        print(cmd_err)

        ## 示例代码2
        import subprocess
        obj2 = subprocess.Popen('ls /tmp', shell = True, stdout = subprocess.PIPE)
        obj2.stdout.readlines()
        ```
---
# Linux 日志分析
- ## 基础知识
  - ### `/var/log/cron`
    - #### 日志任务, 记录系统的定时任务
  - ### `/var/log/cups`
    - #### 打印信息的日志
  - ### `/var/log/message`
    - #### 记录系统重要信息的日志
  - ### `/var/log/lastlog`  
    - #### 最后一次登录
  - ### ==/var/log/secure==
    - #### 登录日志
  - ### `/var/log/utmp`
    - #### 目前登录用户的信息
  - ### `/var/log/wtmp`
    - #### 成功登录的二进制文件
  - ### `/var/log/btmp`
    - #### 登录失败的二进制文件
  - ### `/etc/hosts.deny`
    - #### host黑名单
  - ### `/etc/hosts.allow`
    - #### host白名单
---
# Linux 日志分析技巧
- ## 基础命令
  - ### find
    - #### `find / -name xxx`
      - ##### 找到带有xxx的文件
  - ### grep
    - #### `cat /etc/passwd | grep 'root'`
    - #### `cat /etc/passwd | grep -C 5 'root'`
      - ##### 显示root上下各五行
  - ### awk
    - #### `cat /etc/passwd | awk -F ':' {print $1}`
  - ### sed
  - ### cat
  - ### tail
    - #### `cat /etc/passwd | tail -n +10 | head -n 5`
      - ##### 显示第十行到第十五行
  - ### head
    - #### `cat /etc/passwd | tail -n +10 | head -n 5`
      - ##### 显示第十行到第十五行


